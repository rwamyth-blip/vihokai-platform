# Local development only. Never commit production secrets.
API_SECRET=change-me
# ชี้ production ชั่วคราวเพื่อทดสอบ passthrough ของ Worker
# (อย่าใช้ค่านี้ตอน deploy — production ใช้ vars ใน wrangler.jsonc/dashboard)
BACKEND_URL=https://api.vihokai.com
